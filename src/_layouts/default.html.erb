<!doctype html>
<html lang="<%= site.locale || 'en' %>">
<head>
<%= render "head", metadata: site.metadata, title: (data.title || "Index") %>
</head>
<body class="<%= [data.layout, data.page_class].compact.join(' ') %>">
<%= render "blobs", metadata: site.metadata %>

<header>
  <h1>
    <a href="/">
      <img src="/logo.svg" alt="Company Logo">
      Eli <i>"fetsh"</i> Heiman
    </a>
  </h1>
</header>

<main>
<%= yield %>
</main>

<%= render "footer", metadata: site.metadata %>
</body>
</html>
